{% extends '@Drop/base.html.twig' %}
{% block main_content %}
    <div id="product-page" class="container">
        <div class="row">
            <div class="col-md-8">
                <div class="row">
                    <div class="col-md-12 name">
                        <p>{{ product.name }}</p>
                    </div>
                </div>
                <div class=row">
                    <div class="col-md-2 little-page">
                        <ul>
                            {% set i = 0 %}
                            {% for image in product.images %}
                            <li>
                                <div class="product-img" onmouseover="changeImage('min-img{{ i }}');">
                                    <img src="{{ asset('assets/product_images/products/' ~ product.images[i]) }}" id="min-img{{ i }}" alt=""/>
                                </div>
                            </li>
                                {% set i = ( i | default(1) ) + 1 %}
                            {% endfor %}
                        </ul>
                    </div>
                    <div class="col-md-10 main-image">
                        <img src="{{ asset('assets/product_images/products/' ~ product.images['0']) }}" id="main-img" alt=""/>
                    </div>
                    <!-- The Modal -->
                    <div id="myModal" class="modal">
                        <span class="close">&times;</span>
                        <img class="modal-content" id="img01">
                        <div id="caption"></div>
                    </div>
                </div>
            </div>
            <div class="col-md-4 parameters">
                <div class="row">
                    <div class="col-md-6 price">
                        <span>{{ product.cost }}</span>
                        <span>грн</span>
                    </div>
                    <div class="col-md-6">
                        <a href="{{ path('user_action.new_order', {'id': product.id}) }}" class="btn btn-success btn-lg">Заказать</a>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12 product-id">Номер товара: {{ product.id }}</div>
                </div>
                <div class="row description">
                    <p>Описание :</p>
                    <p>
                        {{ product.description }}
                    </p>
                </div>
            </div>
        </div>
    </div>
{% endblock main_content %}
{% block specially_script %}
    <script src="{{ asset('build/one-product-page.js') }}" defer></script>
{% endblock specially_script %}

